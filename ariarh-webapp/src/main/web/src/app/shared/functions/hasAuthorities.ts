// // Verifier l'autorisation de user connecté
// export function hasAuthorities(authorities: any[] | undefined, authority: string): boolean {
//     let test;
//     test = authorities.find(auth => {
//         if (auth.authority === authority) {
//             return true;
//         }
//     });
//     return test != null;
//
//
// }
